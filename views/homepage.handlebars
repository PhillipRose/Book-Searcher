<!-- Loop over each user object -->
{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <!-- Render user-specific data with Handlebars.js expressions -->
    <h2>{{{get_emoji}}} {{user.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}}


{{!-- This will show the user their current Want to Read Books List if they are logged in or will prompt them to login if they are not. --}}

{{!-- This will also have the button options to mark a book as completed or delete the book from their list.  --}}
