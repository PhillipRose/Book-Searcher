{{!-- This will show a login box the user by default but can change into a register box when a "register" button/link is clicked. There should also be a "login" button/link on the register box to change it back to the login box --}}
{{!-- The login box will have email and password fields and a login button. --}}
{{!-- The register box will have a username, email and password field along with a register button. --}}